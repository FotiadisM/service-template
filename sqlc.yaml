version: "2"
sql:
  - engine: "postgresql"
    queries: "./internal/store/queries.sql"
    schema: "./internal/store/schema.sql"
    gen:
      go:
        package: "store"
        out: "./internal/store/"
        emit_interface: true
        emit_empty_slices: true
